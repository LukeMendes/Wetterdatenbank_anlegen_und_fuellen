# Wetterdatenbank_anlegen_und_fuellen
Ein SQL-Skript, das eine Beispieldatenbank mit Wetterdaten anlegt und mit Werten füllt. Diese Datenbank enthällt die Tabellen "wettermassung" und "wetterstation", deren Attribute über einen "Foreign Key" miteinander verknüpft sind.
